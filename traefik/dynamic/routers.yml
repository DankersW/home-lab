http:
  routers:
    homer:
      rule: "Host(`home.dankers.io`)"
      service: homer
      entryPoints:
        - web

    paperless:
      rule: "Host(`paperless.dankers.io`)"
      service: paperless
      entryPoints:
        - web
      middlewares:
        - paperless-headers
